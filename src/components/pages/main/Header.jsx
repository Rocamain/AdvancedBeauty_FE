import { Typography } from '@mui/material';

export default function Header() {
  return (
    <div>
      <Typography variant="h1" component="h2">
        Header
      </Typography>
    </div>
  );
}
